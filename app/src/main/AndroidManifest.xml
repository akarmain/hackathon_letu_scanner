<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="false" />

    <!-- Scanning works offline; internet access is used only for product images. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission
        android:name="android.permission.ACCESS_NETWORK_STATE"
        tools:node="remove" />

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:enableOnBackInvokedCallback="true"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Hackathon_letu_scanner">

        <!-- Metadata for device-management and demo builds. Android does not have a
             standard application-description field, so clients may read this value. -->
        <meta-data
            android:name="com.example.hackathon_letu_scanner.APP_DESCRIPTION"
            android:value="@string/app_description" />

        <activity
            android:name=".OperationActivity"
            android:exported="false" />

        <activity
            android:name=".PlacementActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Hackathon_letu_scanner.NoActionBar" />

        <activity
            android:name=".PlacementResultActivity"
            android:exported="false" />

        <activity
            android:name=".ReceivingInvoiceListActivity"
            android:exported="false" />

        <activity
            android:name=".ReceivingTaskActivity"
            android:exported="false" />

        <activity
            android:name=".ReceivingResultActivity"
            android:exported="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
